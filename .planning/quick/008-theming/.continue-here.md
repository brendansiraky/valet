---
task: theming-setup
status: in_progress
last_updated: 2026-01-29
---

<current_state>
Setting up tweakcn themes. Successfully added midnight-bloom theme which is now the default.
</current_state>

<completed_work>
- Added midnight-bloom theme via `npx shadcn@latest add https://tweakcn.com/r/themes/midnight-bloom.json`
- Theme applies to `:root` and `.dark` selectors
- Removed broken `theme-tangerine` class from root.tsx (was referencing non-existent CSS)
</completed_work>

<remaining_work>
- User originally wanted 4 themes added: clean-slate, cosmic-night, cyberpunk, darkmatter
- Need to decide: single theme (current approach) OR multi-theme switcher
- If multi-theme: need to create `.theme-{name}` CSS selectors for each theme
- themes.ts and theme-provider.tsx exist but aren't functional without matching CSS classes
</remaining_work>

<decisions_made>
- tweakcn themes replace `:root`/`.dark` directly - they don't add named theme classes
- For theme switching to work, would need to manually wrap each theme's CSS in `.theme-{name}` selectors
</decisions_made>

<context>
User was frustrated because I broke their uncommitted theme CSS by running npx commands that overwrote app.css. The original theme system (tangerine, bubblegum) was lost. We're starting fresh with midnight-bloom as the base.
</context>

<next_action>
Ask user: Do you want just midnight-bloom as the single theme, or should we set up a multi-theme system with the 4 themes you originally listed?
</next_action>
